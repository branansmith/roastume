const Roastlet = (props) => (
    <div id="roastlet-container">
        <p id="roastlet-title">{props.title}</p>
        <p id ="roastlet-description">{props.roast}</p>
    </div>
)

export default Roastlet;